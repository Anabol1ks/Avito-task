# PR Reviewer Assignment Service

Микросервис для автоматического назначения ревьюеров на Pull Request'ы с возможностью управления командами и участниками.

Тестовое задание для стажёра Backend (осенняя волна 2025) — **Avito**.

---

## 📋 Соответствие ТЗ

### ✅ Основные требования (выполнено)

- [x] **HTTP API** согласно предоставленной OpenAPI-спецификации
- [x] **Автоматическое назначение ревьюеров** — при создании PR назначается до 2 активных ревьюеров из команды автора (исключая самого автора)
- [x] **Переназначение ревьювера** — замена одного ревьювера на случайного активного участника из команды заменяемого
- [x] **Запрет изменений после merge** — после статуса `MERGED` изменение списка ревьюеров невозможно
- [x] **Учёт флага isActive** — неактивные пользователи не назначаются на ревью
- [x] **Идемпотентность merge** — повторный вызов возвращает актуальное состояние без ошибки
- [x] **Docker Compose** — сервис и PostgreSQL поднимаются командой `docker-compose up`
- [x] **Автоматические миграции** — применяются при старте сервиса
- [x] **Порт 8080** — сервис доступен на указанном порту
- [x] **Управление командами** — создание команд с участниками, получение информации о команде
- [x] **Управление пользователями** — изменение статуса `isActive`, получение списка PR пользователя

### ✅ Дополнительные задания (выполнено)

- [x] **Статистика** — эндпоинт `/stats` с количеством назначений по пользователям и PR
- [x] **Нагрузочное тестирование** — k6 тесты с результатами (p95 < 300ms, success rate > 99.9%)
- [x] **Интеграционное тестирование** — полное E2E и unit-тестирование всех компонентов
- [x] **Конфигурация линтера** — настроенный golangci-lint с набором правил

### ❌ Не реализовано

- [ ] **Массовая деактивация пользователей команды** — отсутствует отдельный эндпоинт для массовой деактивации (можно деактивировать только по одному через `/users/setIsActive`)

---

## 🛠 Стек технологий

### Backend
- **Go 1.23** — основной язык разработки
- **Gin** — HTTP роутер и middleware
- **GORM** — ORM для работы с БД
- **PostgreSQL** — основная БД (в тестах используется SQLite in-memory)
- **Zap** — структурированное логирование

### Инфраструктура
- **Docker & Docker Compose** — контейнеризация
- **godotenv** — управление переменными окружения

### Тестирование
- **testify** — библиотека для assertions
- **k6** — нагрузочное тестирование

### Code Quality
- **golangci-lint** — статический анализ кода

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
docker-compose up -d --build
```

Сервис будет доступен на `http://localhost:8080`

### Вариант 2: Через Make

```bash
# Запуск через Docker Compose
make doc

# Пересборка только app контейнера
make doc-rebuild
```

### Вариант 3: Локальный запуск

**Требования:**
- Go 1.23+
- PostgreSQL 14+
- Make (опционально)

**Шаги:**

1. Запустите PostgreSQL локально
2. Создайте `.env` файл (или используйте значения по умолчанию)
3. Запустите сервис:

```bash
# Через Make
make run

# Или напрямую
go run ./cmd/app/main.go
```

---

## ⚙️ Конфигурация

Сервис конфигурируется через переменные окружения. Все параметры **опциональны** и имеют значения по умолчанию.

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `APP_PORT` | Порт HTTP сервера | `8080` |
| `ENV` | Режим работы (`development`/`production`) | `production` |
| `DB_HOST` | Хост PostgreSQL | `localhost` |
| `DB_PORT` | Порт PostgreSQL | `5432` |
| `DB_USER` | Пользователь БД | `reviewer` |
| `DB_PASSWORD` | Пароль БД | `12341` |
| `DB_NAME` | Имя БД | `reviewer-pr-db` |
| `DB_SSLMODE` | SSL режим для PostgreSQL | `disable` |

### ⚠️ Важно для локального запуска

При запуске **через Docker Compose** используйте:
```bash
DB_HOST=postgres  # имя сервиса из docker-compose.yml
```

При **локальном запуске** (с PostgreSQL на хосте):
```bash
DB_HOST=localhost
```

### Пример `.env` файла

```env
ENV=development
APP_PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=reviewer
DB_PASSWORD=12341
DB_NAME=reviewer-pr-db
DB_SSLMODE=disable
```

---

## 📖 API и бизнес-логика

### OpenAPI спецификация

Полная спецификация доступна в файле [`api/openapi.yml`](./api/openapi.yml).

**Swagger UI:** `http://localhost:8080/swagger/index.html`  
**OpenAPI YAML:** `http://localhost:8080/openapi.yml`

### Ключевые эндпоинты

#### 🏢 Управление командами

- **POST** `/team/add` — создание команды с участниками
- **GET** `/team/get?team_name={name}` — получение информации о команде

#### 👤 Управление пользователями

- **POST** `/users/setIsActive` — изменение статуса активности пользователя
- **GET** `/users/getReview?user_id={id}` — получение списка PR, назначенных пользователю

#### 🔀 Управление Pull Request'ами

- **POST** `/pullRequest/create` — создание PR с автоматическим назначением до 2 ревьюеров
- **POST** `/pullRequest/merge` — перевод PR в статус MERGED (идемпотентная операция)
- **POST** `/pullRequest/reassign` — переназначение ревьювера на активного участника команды

#### 📊 Статистика

- **GET** `/stats` — статистика назначений по пользователям и PR

### Основная бизнес-логика

#### 1. Создание PR и автоназначение ревьюеров

При создании PR (`/pullRequest/create`):
1. Находятся все **активные** участники команды автора
2. Автор **исключается** из списка кандидатов
3. Случайным образом выбирается **до 2 ревьюеров**
4. Если активных участников меньше 2, назначается доступное количество (0/1/2)

#### 2. Переназначение ревьювера

При переназначении (`/pullRequest/reassign`):
1. Проверяется, что PR не в статусе `MERGED`
2. Проверяется, что `old_reviewer_id` действительно назначен на этот PR
3. Находятся активные участники **команды заменяемого ревьювера** (исключая автора PR и текущих ревьюеров)
4. Случайным образом выбирается новый ревьювер
5. Замена происходит в транзакции

#### 3. Merge PR (идемпотентность)

При мерже (`/pullRequest/merge`):
1. Если PR уже в статусе `MERGED` — возвращается текущее состояние без ошибки
2. Если PR в статусе `OPEN` — устанавливается статус `MERGED` и время merge'а
3. После merge'а переназначение ревьюеров **запрещено** (возвращается ошибка `409 PR_MERGED`)

#### 4. Деактивация пользователя

При изменении `isActive` на `false`:
- Пользователь **не удаляется** из уже назначенных PR
- Пользователь **не участвует** в новых назначениях
- Пользователь **не доступен** для переназначения

---

## 🧪 Тестирование

### Интеграционные тесты

Запуск всех тестов:

```bash
# Через Make
make test

# Напрямую
go test -v ./test/... -count=1
```

**Покрытие:**
- ✅ Полный workflow через HTTP API
- ✅ Создание команд и пользователей
- ✅ Создание и merge PR
- ✅ Переназначение ревьюеров
- ✅ Деактивация пользователей
- ✅ Получение статистики
- ✅ Обработка ошибок и edge cases

### Нагрузочное тестирование (k6)

**Требования:** установленный [k6](https://k6.io/)

```bash
# Через Make
make k6-test

# Напрямую
k6 run k6/load_test.js
```

### 📊 Результаты нагрузочного тестирования

**Параметры теста:**
- **VUS (виртуальные пользователи):** 20
- **Длительность:** 30 секунд
- **Всего запросов:** 2281 (≈76 RPS)

**Результаты:**

| Метрика | Требование | Результат | Статус |
|---------|-----------|-----------|--------|
| **p(95) latency** | < 300ms | **48.45ms** | ✅ **6x лучше** |
| **Success rate** | > 99.9% | **100%** | ✅ **Идеально** |

```
THRESHOLDS
  http_req_duration
  ✓ 'p(95)<300' p(95)=48.45ms

  http_req_failed
  ✓ 'rate<0.001' rate=0.00%

TOTAL RESULTS
  checks_total.......: 2281    74.515083/s
  checks_succeeded...: 100.00% 2281 out of 2281
  checks_failed......: 0.00%   0 out of 2281

HTTP
  http_req_duration..............: avg=15.01ms  min=1.52ms   med=8.1ms    max=151.15ms p(90)=33.97ms  p(95)=48.45ms
  http_req_failed................: 0.00%  0 out of 2281
  http_reqs......................: 2281   74.515083/s

EXECUTION
  iteration_duration.............: avg=530.94ms min=508.84ms med=523.85ms max=665.73ms p(90)=560.61ms p(95)=581.31ms
  iterations.....................: 1140   37.241207/s
  vus............................: 20     min=20        max=20

NETWORK
  data_received..................: 34 MB  1.1 MB/s
  data_sent......................: 367 kB 12 kB/s
```

**Выводы:**
- Средний latency **15ms** — отличная производительность
- p95 latency **48.45ms** — в **6 раз лучше** требуемых 300ms
- **0% ошибок** — система полностью стабильна под нагрузкой
- Обработано **2281 запросов** за 30 секунд без единого сбоя

---

## 🔍 Линтер

Проект использует **golangci-lint** с настроенными правилами.

### Запуск

```bash
# Через Make
make lint

# Напрямую
golangci-lint run ./... -v
```

### Конфигурация

Файл конфигурации: [`.golangci.yml`](./.golangci.yml)

**Включенные линтеры:**
- `revive` — проверка стиля кода
- `staticcheck` — статический анализ
- `gosec` — анализ безопасности
- `ineffassign` — поиск неэффективных присваиваний
- `errcheck` — проверка обработки ошибок
- `govet` — встроенный анализатор Go

**Исключения:**
- Отключена проверка `errcheck` в тестовых файлах для `json.Unmarshal`
- Отключена проверка `Close()` в `testhelpers/`

---

## 📁 Структура проекта

```
.
├── api/                    # OpenAPI спецификация
│   ├── api.go
│   └── openapi.yml
├── cmd/
│   └── app/
│       └── main.go        # Точка входа
├── internal/
│   ├── config/            # Конфигурация
│   ├── database/          # Подключение и миграции БД
│   ├── http/              # HTTP handlers и DTO
│   ├── logger/            # Zap logger
│   ├── models/            # GORM модели
│   ├── repository/        # Слой БД
│   ├── router/            # Маршрутизация
│   ├── service/           # Бизнес-логика
│   └── testhelpers/       # Утилиты для тестов
├── k6/
│   └── load_test.js       # Нагрузочные тесты
├── test/                  # Интеграционные тесты
├── docker-compose.yml
├── Dockerfile
├── makefile
└── README.md
```

### Архитектурные решения

**Clean Architecture** — проект разделён на слои:
1. **HTTP** (`internal/http`) — обработка запросов, DTO, маппинг ошибок
2. **Service** (`internal/service`) — бизнес-логика
3. **Repository** (`internal/repository`) — работа с БД
4. **Models** (`internal/models`) — доменные модели

**Преимущества:**
- Чёткое разделение ответственности
- Лёгкость тестирования (каждый слой тестируется независимо)
- Простота расширения и поддержки

---

## 🤔 Принятые решения и допущения

### 1. Случайный выбор ревьюеров

**Вопрос:** Как именно распределять ревьюеров при наличии нескольких кандидатов?

**Решение:** Используется псевдослучайный выбор (`math/rand` с seed от времени). Это обеспечивает:
- Равномерное распределение нагрузки
- Простоту реализации
- Предсказуемость в тестах (можно зафиксировать seed)

### 2. Идемпотентность merge

**Вопрос:** Что возвращать при повторном merge?

**Решение:** Повторный вызов `/pullRequest/merge` для уже смерженного PR возвращает `200 OK` с актуальным состоянием. Это:
- Соответствует определению идемпотентности
- Упрощает логику клиента (можно безопасно повторять запрос)

### 3. Переназначение из команды заменяемого

**Вопрос:** Из какой команды брать нового ревьювера?

**Решение:** Согласно ТЗ — из команды **заменяемого** ревьювера. Исключаются:
- Автор PR
- Текущие ревьюеры (чтобы не назначить одного и того же человека)

### 4. Поведение при деактивации

**Вопрос:** Что делать с уже назначенными PR при деактивации?

**Решение:** 
- Пользователь **остаётся** ревьювером на уже назначенных PR
- Новые назначения **не происходят**
- Переназначение на деактивированного **невозможно**

Это логично, так как:
- Деактивация может быть временной (отпуск, больничный)
- Человек мог уже начать ревью
- Автоматическое переназначение может нарушить процесс

### 5. SQLite для тестов

**Вопрос:** Какую БД использовать в тестах?

**Решение:** In-memory SQLite для:
- Скорости выполнения тестов
- Изоляции тестов
- Отсутствия необходимости в внешних зависимостях

Production использует PostgreSQL.

---

## 📝 Makefile команды

```bash
make run          # Локальный запуск
make doc          # Docker Compose up
make doc-rebuild  # Пересборка app контейнера
make test         # Запуск тестов
make k6-test      # Нагрузочное тестирование
make lint         # Запуск линтера
```

---

## 📄 Лицензия

[MIT License](./LICENSE)

---

## 👤 Автор

**Grigor** — Тестовое задание для Avito (осень 2025)

**GitHub:** [Anabol1ks/Avito-task](https://github.com/Anabol1ks/Avito-task)